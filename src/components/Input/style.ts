import { StyleSheet } from "react-native"

export const style = StyleSheet.create({
    input: {
        borderColor: '#808080',
        borderWidth: 0.5,
        borderRadius: 10,
        fontSize: 16,
        padding: 8,
        minWidth: 320,
        textAlign: 'center'
    }
})